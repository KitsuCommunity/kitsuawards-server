<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="user-scalable=no, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, minimal-ui"
    />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <title>GraphQL Playground</title>

    <%= stylesheet_link_tag 'playground', media: 'all' %>
    <%= javascript_include_tag 'middleware' %>
  </head>

  <body>
    <div id="app">
      <style nonce="<%= content_security_policy_nonce %>" type="text/css">
        body {
          background-color: rgb(23, 42, 58);
          font-family: Open Sans, sans-serif;
          height: 90vh;
        }

        #app {
          height: 100%;
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: center;
        }

        .loading {
          font-size: 32px;
          font-weight: 200;
          color: rgba(255, 255, 255, 0.6);
          margin-left: 20px;
        }

        img {
          width: 78px;
          height: 78px;
        }

        .title {
          font-weight: 400;
        }
      </style>

      <img
        alt=""
        src="https://cdn.jsdelivr.net/npm/graphql-playground-react/build/logo.png"
      />

      <div class="loading">
        Loading
        <span class="title">GraphQL Playground</span>
      </div>
    </div>

    <script
      nonce="<%= content_security_policy_nonce %>"
      type="text/javascript"
    >
      window.addEventListener('load', (event) => {
        GraphQLPlayground.init(document.getElementById('app'), {
          endpoint: '/graphql'
        })
      })
    </script>
  </body>
</html>
